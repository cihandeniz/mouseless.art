## Brainstuck

Interactive Brainfuck JS interpreter with audio visualization.

> You can modify the array in `codes.js` you want to add more brainfuck program.

Access specific programs using the `id` parameter:

```
?id={int}
```